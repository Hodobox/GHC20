# Google Haschode 2020: Gigabrains

inputs/: contains test data

outputs/: contains solver outputs (in gitignore)

build.sh: bash script for compiling (compiles to sol)

code/: contains code

code/includes.h: global includes

code/main.cpp: main

code/parser.cpp: I/O

code/solver.cpp: attempts at solving problem

code/verifier.cpp: takes input and solution, verifies and scores it

